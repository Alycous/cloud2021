server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #server-addr: localhost:8848 #配置Nacos地址
        server-addr: 192.168.60.132:1111 #配置Nacos地址 集群版

management:
  endpoints:
    web:
      exposure:
        include: '*'

